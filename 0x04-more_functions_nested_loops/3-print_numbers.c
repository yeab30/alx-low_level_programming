#include "main.h"
/**
 * print_number - print the no since o up to 9
 * Return: the no since 0 - 9
 */
void print_numbers(void)
{
	    int x;
	        for (x = 0; x <= 9; x++)
			    {
				            _putchar(x + '0');
					        }
		    _putchar('\n');
}

